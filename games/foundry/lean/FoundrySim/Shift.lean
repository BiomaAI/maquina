-- Generated by scripts/generate-foundry-shift.py; edit the phase recipes there.
import FoundrySim.ControlRoom

/-! A longer, player-directed production shift. Every branch is an ordinary
OperationProposal resolved by Maquina; these declarations never mutate holdings. -/

namespace Maquina.Games.Foundry.Shift
open ControlRoom Refuel Simulation

private def cancelActive (station : Workcell.Station) : Workcell.Intent :=
  intent station Refuel.cancelActiveRefuel

def snapshot0 : Snapshot := rootSnapshot

def node0 : ControlRoom.Node where
  snapshot := snapshot0
  title := "The night shift is yours"
  summary := "Deliver up to 20 L across two service lines. One operator, one labor unit, and one output bay per line. Balance throughput against reserves."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10000 (enter .primary), label := "Staff Primary", detail := "Give the primary service line the shift’s only operator." },
      { candidate := candidate 10470 (enter .secondary), label := "Staff Secondary", detail := "Give the secondary service line the shift’s only operator." } ]
  candidateIdsUnique := by decide

def orders1_0 : OrderSet Workcell.Intent where
  orders := [ order 10000 10 0 (enter .primary) ]
  idsUnique := by decide
def run1_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot0 ⟨10010⟩ rfl orders1_0

def snapshot1 : Snapshot := run1_0.child

def node1 : ControlRoom.Node where
  snapshot := snapshot1
  title := "Operator on station"
  summary := "Choose whether to release work. Your operator cannot staff both stations at once."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10010 (reserve .primary), label := "Release the first lot", detail := "Reserve 10 L. Keep 10 L available for a second order." },
      { candidate := candidate 10460 (leave .primary), label := "Stand down", detail := "Return the operator and preserve the full 20 L supply." },
      { candidate := candidate 90001 (enter .secondary), label := "Staff both stations", detail := "Rejected: the unique operator is already in custody." } ]
  candidateIdsUnique := by decide

def orders2_0 : OrderSet Workcell.Intent where
  orders := [ order 10010 10 0 (reserve .primary) ]
  idsUnique := by decide
def run2_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot1 ⟨10020⟩ rfl orders2_0

def snapshot2 : Snapshot := run2_0.child

def node2 : ControlRoom.Node where
  snapshot := snapshot2
  title := "First lot reserved"
  summary := "The first 10 L is reserved. Start the work or unwind the order."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10020 (dispatch .primary), label := "Start production", detail := "Lock the operator and labor to the first lot." },
      { candidate := candidate 10440 (dispatch .primary), label := "Dispatch", detail := "Both orders are valid now. Dispatch wins arbitration; departure loses. Inspect automatic failure and safe recovery." },
      { candidate := candidate 10441 (leave .primary), label := "Leave", detail := "Both orders are valid now. Dispatch wins arbitration; departure loses. Inspect automatic failure and safe recovery." },
      { candidate := candidate 10450 (cancel .primary), label := "Cancel the order", detail := "Return the reservation and release the operator." } ]
  candidateIdsUnique := by decide

def orders3_0 : OrderSet Workcell.Intent where
  orders := [ order 10020 10 0 (dispatch .primary) ]
  idsUnique := by decide
def run3_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot2 ⟨10030⟩ rfl orders3_0

def snapshot3 : Snapshot := run3_0.child

def node3 : ControlRoom.Node where
  snapshot := snapshot3
  title := "Production is live"
  summary := "Labor and the operator are busy. Queue ahead for throughput, or finish one lot before committing more fuel."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10030 (reserve .primary), label := "Build a production pipeline", detail := "Reserve the second lot while the first is active. All fuel will be committed." },
      { candidate := candidate 10190 (advance .primary), label := "Finish the first lot", detail := "Complete one lot before deciding whether to accept another." },
      { candidate := candidate 90003 (leave .primary), label := "Leave active work", detail := "Rejected: active custody keeps the operator with the running lot." } ]
  candidateIdsUnique := by decide

def orders4_0 : OrderSet Workcell.Intent where
  orders := [ order 10030 10 0 (reserve .primary) ]
  idsUnique := by decide
def run4_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot3 ⟨10040⟩ rfl orders4_0

def snapshot4 : Snapshot := run4_0.child

def node4 : ControlRoom.Node where
  snapshot := snapshot4
  title := "Two lots, one labor slot"
  summary := "The pipeline is full: 10 L active and 10 L reserved. Complete the first order to return the shared labor."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10040 (advance .primary), label := "Finish with backlog ready", detail := "The first lot moves to output. The next lot waits for labor." } ]
  candidateIdsUnique := by decide

def orders5_0 : OrderSet Workcell.Intent where
  orders := [ order 10040 10 0 (advance .primary) ]
  idsUnique := by decide
def run5_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot4 ⟨10050⟩ rfl orders5_0

def orders5_1 : OrderSet Workcell.Intent where
  orders := [ order 10043 10 0 (complete .primary) ]
  idsUnique := by decide
def run5_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run5_0.child ⟨10051⟩ rfl orders5_1

def snapshot5 : Snapshot := run5_1.child

def node5 : ControlRoom.Node where
  snapshot := snapshot5
  title := "The output bay is the bottleneck"
  summary := "Both bays contain work. Collect and dispatch together, or explore what happens when output remains blocked."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10050 (collect .primary), label := "Collect", detail := "Deliver the first lot and start the second in one simultaneous order set." },
      { candidate := candidate 10051 (dispatch .primary), label := "Dispatch", detail := "Deliver the first lot and start the second in one simultaneous order set." },
      { candidate := candidate 10100 (dispatch .primary), label := "Dispatch into backpressure", detail := "Start the next lot before clearing output. You must free the bay before completion." },
      { candidate := candidate 10180 (cancel .primary), label := "Cancel backlog and cash out", detail := "Return the second reservation and deliver the first lot." } ]
  candidateIdsUnique := by decide

def orders6_0 : OrderSet Workcell.Intent where
  orders := [ order 10050 10 0 (collect .primary), order 10051 10 1 (dispatch .primary) ]
  idsUnique := by decide
def run6_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot5 ⟨10060⟩ rfl orders6_0

def snapshot6 : Snapshot := run6_0.child

def node6 : ControlRoom.Node where
  snapshot := snapshot6
  title := "The final lot is running"
  summary := "The first lot has been delivered. Finish the second or cancel it safely before delivery."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10060 (advance .primary), label := "Finish the second lot", detail := "Advance the final unit of work. The operator remains in active custody." },
      { candidate := candidate 10090 (cancelActive .primary), label := "Cancel active work safely", detail := "Return the final lot’s fuel and labor, then release the operator." },
      { candidate := candidate 90006 (leave .primary), label := "Leave active work", detail := "Rejected: active custody keeps the operator with the running lot." } ]
  candidateIdsUnique := by decide

def orders7_0 : OrderSet Workcell.Intent where
  orders := [ order 10060 10 0 (advance .primary) ]
  idsUnique := by decide
def run7_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot6 ⟨10070⟩ rfl orders7_0

def snapshot7 : Snapshot := run7_0.child

def node7 : ControlRoom.Node where
  snapshot := snapshot7
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10070 (complete .primary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders8_0 : OrderSet Workcell.Intent where
  orders := [ order 10070 10 0 (complete .primary) ]
  idsUnique := by decide
def run8_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot7 ⟨10080⟩ rfl orders8_0

def snapshot8 : Snapshot := run8_0.child

def node8 : ControlRoom.Node where
  snapshot := snapshot8
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10080 (collect .primary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10081 (leave .primary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders9_0 : OrderSet Workcell.Intent where
  orders := [ order 10080 10 0 (collect .primary), order 10081 10 1 (leave .primary) ]
  idsUnique := by decide
def run9_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot8 ⟨10090⟩ rfl orders9_0

def snapshot9 : Snapshot := run9_0.child

def node9 : ControlRoom.Node where
  snapshot := snapshot9
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders10_0 : OrderSet Workcell.Intent where
  orders := [ order 10090 10 0 (cancelActive .primary) ]
  idsUnique := by decide
def run10_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot6 ⟨10100⟩ rfl orders10_0

def orders10_1 : OrderSet Workcell.Intent where
  orders := [ order 10093 10 0 (leave .primary) ]
  idsUnique := by decide
def run10_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run10_0.child ⟨10101⟩ rfl orders10_1

def snapshot10 : Snapshot := run10_1.child

def node10 : ControlRoom.Node where
  snapshot := snapshot10
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders11_0 : OrderSet Workcell.Intent where
  orders := [ order 10100 10 0 (dispatch .primary) ]
  idsUnique := by decide
def run11_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot5 ⟨10110⟩ rfl orders11_0

def snapshot11 : Snapshot := run11_0.child

def node11 : ControlRoom.Node where
  snapshot := snapshot11
  title := "Work in progress. Output occupied."
  summary := "The second lot is active, but the first still occupies output. Choose when to clear it."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10110 (advance .primary), label := "Work before clearing the bay", detail := "Finish the work, leaving the output bay occupied. Completion will be blocked." },
      { candidate := candidate 10150 (collect .primary), label := "Collect", detail := "Clear the first lot and advance the second at the same logical tick." },
      { candidate := candidate 10151 (advance .primary), label := "Advance", detail := "Clear the first lot and advance the second at the same logical tick." } ]
  candidateIdsUnique := by decide

def orders12_0 : OrderSet Workcell.Intent where
  orders := [ order 10110 10 0 (advance .primary) ]
  idsUnique := by decide
def run12_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot11 ⟨10120⟩ rfl orders12_0

def snapshot12 : Snapshot := run12_0.child

def node12 : ControlRoom.Node where
  snapshot := snapshot12
  title := "Completion is blocked"
  summary := "Work is complete, but the one-slot output queue is full. Clear the first output before completing the next."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10120 (collect .primary), label := "Clear the output bottleneck", detail := "Deliver the first lot to make room for the completed second lot." },
      { candidate := candidate 90012 (complete .primary), label := "Complete into a full bay", detail := "Rejected: output capacity must be freed before completion." } ]
  candidateIdsUnique := by decide

def orders13_0 : OrderSet Workcell.Intent where
  orders := [ order 10120 10 0 (collect .primary) ]
  idsUnique := by decide
def run13_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot12 ⟨10130⟩ rfl orders13_0

def snapshot13 : Snapshot := run13_0.child

def node13 : ControlRoom.Node where
  snapshot := snapshot13
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10130 (complete .primary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders14_0 : OrderSet Workcell.Intent where
  orders := [ order 10130 10 0 (complete .primary) ]
  idsUnique := by decide
def run14_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot13 ⟨10140⟩ rfl orders14_0

def snapshot14 : Snapshot := run14_0.child

def node14 : ControlRoom.Node where
  snapshot := snapshot14
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10140 (collect .primary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10141 (leave .primary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders15_0 : OrderSet Workcell.Intent where
  orders := [ order 10140 10 0 (collect .primary), order 10141 10 1 (leave .primary) ]
  idsUnique := by decide
def run15_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot14 ⟨10150⟩ rfl orders15_0

def snapshot15 : Snapshot := run15_0.child

def node15 : ControlRoom.Node where
  snapshot := snapshot15
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders16_0 : OrderSet Workcell.Intent where
  orders := [ order 10150 10 0 (collect .primary), order 10151 10 1 (advance .primary) ]
  idsUnique := by decide
def run16_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot11 ⟨10160⟩ rfl orders16_0

def snapshot16 : Snapshot := run16_0.child

def node16 : ControlRoom.Node where
  snapshot := snapshot16
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10160 (complete .primary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders17_0 : OrderSet Workcell.Intent where
  orders := [ order 10160 10 0 (complete .primary) ]
  idsUnique := by decide
def run17_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot16 ⟨10170⟩ rfl orders17_0

def snapshot17 : Snapshot := run17_0.child

def node17 : ControlRoom.Node where
  snapshot := snapshot17
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10170 (collect .primary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10171 (leave .primary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders18_0 : OrderSet Workcell.Intent where
  orders := [ order 10170 10 0 (collect .primary), order 10171 10 1 (leave .primary) ]
  idsUnique := by decide
def run18_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot17 ⟨10180⟩ rfl orders18_0

def snapshot18 : Snapshot := run18_0.child

def node18 : ControlRoom.Node where
  snapshot := snapshot18
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders19_0 : OrderSet Workcell.Intent where
  orders := [ order 10180 10 0 (cancel .primary) ]
  idsUnique := by decide
def run19_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot5 ⟨10190⟩ rfl orders19_0

def orders19_1 : OrderSet Workcell.Intent where
  orders := [ order 10183 10 0 (collect .primary) ]
  idsUnique := by decide
def run19_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run19_0.child ⟨10191⟩ rfl orders19_1

def orders19_2 : OrderSet Workcell.Intent where
  orders := [ order 10186 10 0 (leave .primary) ]
  idsUnique := by decide
def run19_2 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run19_1.child ⟨10192⟩ rfl orders19_2

def snapshot19 : Snapshot := run19_2.child

def node19 : ControlRoom.Node where
  snapshot := snapshot19
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders20_0 : OrderSet Workcell.Intent where
  orders := [ order 10190 10 0 (advance .primary) ]
  idsUnique := by decide
def run20_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot3 ⟨10200⟩ rfl orders20_0

def orders20_1 : OrderSet Workcell.Intent where
  orders := [ order 10193 10 0 (complete .primary) ]
  idsUnique := by decide
def run20_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run20_0.child ⟨10201⟩ rfl orders20_1

def snapshot20 : Snapshot := run20_1.child

def node20 : ControlRoom.Node where
  snapshot := snapshot20
  title := "The first lot is ready"
  summary := "The first lot occupies output custody. Deliver it now or reserve your remaining supply."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10200 (collect .primary), label := "Deliver the first order", detail := "Collect 10 L and two service credits. Keep the operator for a possible second lot." },
      { candidate := candidate 10290 (reserve .primary), label := "Queue before collection", detail := "Commit the remaining fuel while the first output still occupies its bay." } ]
  candidateIdsUnique := by decide

def orders21_0 : OrderSet Workcell.Intent where
  orders := [ order 10200 10 0 (collect .primary) ]
  idsUnique := by decide
def run21_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot20 ⟨10210⟩ rfl orders21_0

def snapshot21 : Snapshot := run21_0.child

def node21 : ControlRoom.Node where
  snapshot := snapshot21
  title := "Bank the shift or keep going?"
  summary := "You have delivered 10 L. Finish conservatively, or commit the remaining fuel to double production."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10210 (leave .primary), label := "Close the shift at 10 L", detail := "Bank two credits and return the operator. Preserve 10 L for tomorrow." },
      { candidate := candidate 10220 (reserve .primary), label := "Take the second order", detail := "Commit the remaining 10 L for another two service credits." } ]
  candidateIdsUnique := by decide

def orders22_0 : OrderSet Workcell.Intent where
  orders := [ order 10210 10 0 (leave .primary) ]
  idsUnique := by decide
def run22_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot21 ⟨10220⟩ rfl orders22_0

def snapshot22 : Snapshot := run22_0.child

def node22 : ControlRoom.Node where
  snapshot := snapshot22
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders23_0 : OrderSet Workcell.Intent where
  orders := [ order 10220 10 0 (reserve .primary) ]
  idsUnique := by decide
def run23_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot21 ⟨10230⟩ rfl orders23_0

def snapshot23 : Snapshot := run23_0.child

def node23 : ControlRoom.Node where
  snapshot := snapshot23
  title := "One last order"
  summary := "The second lot is funded. Dispatch it or preserve the reserve."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10230 (dispatch .primary), label := "Run the second lot", detail := "Use the returned labor for the final order." },
      { candidate := candidate 10280 (cancel .primary), label := "Protect the reserve", detail := "Cancel the second lot, return its fuel, and close with 10 L delivered." } ]
  candidateIdsUnique := by decide

def orders24_0 : OrderSet Workcell.Intent where
  orders := [ order 10230 10 0 (dispatch .primary) ]
  idsUnique := by decide
def run24_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot23 ⟨10240⟩ rfl orders24_0

def snapshot24 : Snapshot := run24_0.child

def node24 : ControlRoom.Node where
  snapshot := snapshot24
  title := "The final lot is running"
  summary := "The first lot has been delivered. Finish the second or cancel it safely before delivery."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10240 (advance .primary), label := "Finish the second lot", detail := "Advance the final unit of work. The operator remains in active custody." },
      { candidate := candidate 10270 (cancelActive .primary), label := "Cancel active work safely", detail := "Return the final lot’s fuel and labor, then release the operator." },
      { candidate := candidate 90024 (leave .primary), label := "Leave active work", detail := "Rejected: active custody keeps the operator with the running lot." } ]
  candidateIdsUnique := by decide

def orders25_0 : OrderSet Workcell.Intent where
  orders := [ order 10240 10 0 (advance .primary) ]
  idsUnique := by decide
def run25_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot24 ⟨10250⟩ rfl orders25_0

def snapshot25 : Snapshot := run25_0.child

def node25 : ControlRoom.Node where
  snapshot := snapshot25
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10250 (complete .primary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders26_0 : OrderSet Workcell.Intent where
  orders := [ order 10250 10 0 (complete .primary) ]
  idsUnique := by decide
def run26_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot25 ⟨10260⟩ rfl orders26_0

def snapshot26 : Snapshot := run26_0.child

def node26 : ControlRoom.Node where
  snapshot := snapshot26
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10260 (collect .primary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10261 (leave .primary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders27_0 : OrderSet Workcell.Intent where
  orders := [ order 10260 10 0 (collect .primary), order 10261 10 1 (leave .primary) ]
  idsUnique := by decide
def run27_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot26 ⟨10270⟩ rfl orders27_0

def snapshot27 : Snapshot := run27_0.child

def node27 : ControlRoom.Node where
  snapshot := snapshot27
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders28_0 : OrderSet Workcell.Intent where
  orders := [ order 10270 10 0 (cancelActive .primary) ]
  idsUnique := by decide
def run28_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot24 ⟨10280⟩ rfl orders28_0

def orders28_1 : OrderSet Workcell.Intent where
  orders := [ order 10273 10 0 (leave .primary) ]
  idsUnique := by decide
def run28_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run28_0.child ⟨10281⟩ rfl orders28_1

def snapshot28 : Snapshot := run28_1.child

def node28 : ControlRoom.Node where
  snapshot := snapshot28
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders29_0 : OrderSet Workcell.Intent where
  orders := [ order 10280 10 0 (cancel .primary) ]
  idsUnique := by decide
def run29_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot23 ⟨10290⟩ rfl orders29_0

def orders29_1 : OrderSet Workcell.Intent where
  orders := [ order 10283 10 0 (leave .primary) ]
  idsUnique := by decide
def run29_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run29_0.child ⟨10291⟩ rfl orders29_1

def snapshot29 : Snapshot := run29_1.child

def node29 : ControlRoom.Node where
  snapshot := snapshot29
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders30_0 : OrderSet Workcell.Intent where
  orders := [ order 10290 10 0 (reserve .primary) ]
  idsUnique := by decide
def run30_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot20 ⟨10300⟩ rfl orders30_0

def snapshot30 : Snapshot := run30_0.child

def node30 : ControlRoom.Node where
  snapshot := snapshot30
  title := "The output bay is the bottleneck"
  summary := "Both bays contain work. Collect and dispatch together, or explore what happens when output remains blocked."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10300 (collect .primary), label := "Collect", detail := "Deliver the first lot and start the second in one simultaneous order set." },
      { candidate := candidate 10301 (dispatch .primary), label := "Dispatch", detail := "Deliver the first lot and start the second in one simultaneous order set." },
      { candidate := candidate 10350 (dispatch .primary), label := "Dispatch into backpressure", detail := "Start the next lot before clearing output. You must free the bay before completion." },
      { candidate := candidate 10430 (cancel .primary), label := "Cancel backlog and cash out", detail := "Return the second reservation and deliver the first lot." } ]
  candidateIdsUnique := by decide

def orders31_0 : OrderSet Workcell.Intent where
  orders := [ order 10300 10 0 (collect .primary), order 10301 10 1 (dispatch .primary) ]
  idsUnique := by decide
def run31_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot30 ⟨10310⟩ rfl orders31_0

def snapshot31 : Snapshot := run31_0.child

def node31 : ControlRoom.Node where
  snapshot := snapshot31
  title := "The final lot is running"
  summary := "The first lot has been delivered. Finish the second or cancel it safely before delivery."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10310 (advance .primary), label := "Finish the second lot", detail := "Advance the final unit of work. The operator remains in active custody." },
      { candidate := candidate 10340 (cancelActive .primary), label := "Cancel active work safely", detail := "Return the final lot’s fuel and labor, then release the operator." },
      { candidate := candidate 90031 (leave .primary), label := "Leave active work", detail := "Rejected: active custody keeps the operator with the running lot." } ]
  candidateIdsUnique := by decide

def orders32_0 : OrderSet Workcell.Intent where
  orders := [ order 10310 10 0 (advance .primary) ]
  idsUnique := by decide
def run32_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot31 ⟨10320⟩ rfl orders32_0

def snapshot32 : Snapshot := run32_0.child

def node32 : ControlRoom.Node where
  snapshot := snapshot32
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10320 (complete .primary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders33_0 : OrderSet Workcell.Intent where
  orders := [ order 10320 10 0 (complete .primary) ]
  idsUnique := by decide
def run33_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot32 ⟨10330⟩ rfl orders33_0

def snapshot33 : Snapshot := run33_0.child

def node33 : ControlRoom.Node where
  snapshot := snapshot33
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10330 (collect .primary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10331 (leave .primary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders34_0 : OrderSet Workcell.Intent where
  orders := [ order 10330 10 0 (collect .primary), order 10331 10 1 (leave .primary) ]
  idsUnique := by decide
def run34_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot33 ⟨10340⟩ rfl orders34_0

def snapshot34 : Snapshot := run34_0.child

def node34 : ControlRoom.Node where
  snapshot := snapshot34
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders35_0 : OrderSet Workcell.Intent where
  orders := [ order 10340 10 0 (cancelActive .primary) ]
  idsUnique := by decide
def run35_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot31 ⟨10350⟩ rfl orders35_0

def orders35_1 : OrderSet Workcell.Intent where
  orders := [ order 10343 10 0 (leave .primary) ]
  idsUnique := by decide
def run35_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run35_0.child ⟨10351⟩ rfl orders35_1

def snapshot35 : Snapshot := run35_1.child

def node35 : ControlRoom.Node where
  snapshot := snapshot35
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders36_0 : OrderSet Workcell.Intent where
  orders := [ order 10350 10 0 (dispatch .primary) ]
  idsUnique := by decide
def run36_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot30 ⟨10360⟩ rfl orders36_0

def snapshot36 : Snapshot := run36_0.child

def node36 : ControlRoom.Node where
  snapshot := snapshot36
  title := "Work in progress. Output occupied."
  summary := "The second lot is active, but the first still occupies output. Choose when to clear it."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10360 (advance .primary), label := "Work before clearing the bay", detail := "Finish the work, leaving the output bay occupied. Completion will be blocked." },
      { candidate := candidate 10400 (collect .primary), label := "Collect", detail := "Clear the first lot and advance the second at the same logical tick." },
      { candidate := candidate 10401 (advance .primary), label := "Advance", detail := "Clear the first lot and advance the second at the same logical tick." } ]
  candidateIdsUnique := by decide

def orders37_0 : OrderSet Workcell.Intent where
  orders := [ order 10360 10 0 (advance .primary) ]
  idsUnique := by decide
def run37_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot36 ⟨10370⟩ rfl orders37_0

def snapshot37 : Snapshot := run37_0.child

def node37 : ControlRoom.Node where
  snapshot := snapshot37
  title := "Completion is blocked"
  summary := "Work is complete, but the one-slot output queue is full. Clear the first output before completing the next."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10370 (collect .primary), label := "Clear the output bottleneck", detail := "Deliver the first lot to make room for the completed second lot." },
      { candidate := candidate 90037 (complete .primary), label := "Complete into a full bay", detail := "Rejected: output capacity must be freed before completion." } ]
  candidateIdsUnique := by decide

def orders38_0 : OrderSet Workcell.Intent where
  orders := [ order 10370 10 0 (collect .primary) ]
  idsUnique := by decide
def run38_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot37 ⟨10380⟩ rfl orders38_0

def snapshot38 : Snapshot := run38_0.child

def node38 : ControlRoom.Node where
  snapshot := snapshot38
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10380 (complete .primary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders39_0 : OrderSet Workcell.Intent where
  orders := [ order 10380 10 0 (complete .primary) ]
  idsUnique := by decide
def run39_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot38 ⟨10390⟩ rfl orders39_0

def snapshot39 : Snapshot := run39_0.child

def node39 : ControlRoom.Node where
  snapshot := snapshot39
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10390 (collect .primary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10391 (leave .primary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders40_0 : OrderSet Workcell.Intent where
  orders := [ order 10390 10 0 (collect .primary), order 10391 10 1 (leave .primary) ]
  idsUnique := by decide
def run40_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot39 ⟨10400⟩ rfl orders40_0

def snapshot40 : Snapshot := run40_0.child

def node40 : ControlRoom.Node where
  snapshot := snapshot40
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders41_0 : OrderSet Workcell.Intent where
  orders := [ order 10400 10 0 (collect .primary), order 10401 10 1 (advance .primary) ]
  idsUnique := by decide
def run41_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot36 ⟨10410⟩ rfl orders41_0

def snapshot41 : Snapshot := run41_0.child

def node41 : ControlRoom.Node where
  snapshot := snapshot41
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10410 (complete .primary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders42_0 : OrderSet Workcell.Intent where
  orders := [ order 10410 10 0 (complete .primary) ]
  idsUnique := by decide
def run42_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot41 ⟨10420⟩ rfl orders42_0

def snapshot42 : Snapshot := run42_0.child

def node42 : ControlRoom.Node where
  snapshot := snapshot42
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10420 (collect .primary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10421 (leave .primary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders43_0 : OrderSet Workcell.Intent where
  orders := [ order 10420 10 0 (collect .primary), order 10421 10 1 (leave .primary) ]
  idsUnique := by decide
def run43_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot42 ⟨10430⟩ rfl orders43_0

def snapshot43 : Snapshot := run43_0.child

def node43 : ControlRoom.Node where
  snapshot := snapshot43
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders44_0 : OrderSet Workcell.Intent where
  orders := [ order 10430 10 0 (cancel .primary) ]
  idsUnique := by decide
def run44_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot30 ⟨10440⟩ rfl orders44_0

def orders44_1 : OrderSet Workcell.Intent where
  orders := [ order 10433 10 0 (collect .primary) ]
  idsUnique := by decide
def run44_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run44_0.child ⟨10441⟩ rfl orders44_1

def orders44_2 : OrderSet Workcell.Intent where
  orders := [ order 10436 10 0 (leave .primary) ]
  idsUnique := by decide
def run44_2 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run44_1.child ⟨10442⟩ rfl orders44_2

def snapshot44 : Snapshot := run44_2.child

def node44 : ControlRoom.Node where
  snapshot := snapshot44
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders45_0 : OrderSet Workcell.Intent where
  orders := [ order 10440 10 0 (dispatch .primary), order 10441 10 1 (leave .primary) ]
  idsUnique := by decide
def run45_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot2 ⟨10450⟩ rfl orders45_0

def orders45_1 : OrderSet Workcell.Intent where
  orders := [ order 10443 10 0 (fail .primary) ]
  idsUnique := by decide
def run45_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run45_0.child ⟨10451⟩ rfl orders45_1

def orders45_2 : OrderSet Workcell.Intent where
  orders := [ order 10446 10 0 (repair .primary) ]
  idsUnique := by decide
def run45_2 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run45_1.child ⟨10452⟩ rfl orders45_2

def orders45_3 : OrderSet Workcell.Intent where
  orders := [ order 10449 10 0 (start .primary) ]
  idsUnique := by decide
def run45_3 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run45_2.child ⟨10453⟩ rfl orders45_3

def orders45_4 : OrderSet Workcell.Intent where
  orders := [ order 10452 10 0 (leave .primary) ]
  idsUnique := by decide
def run45_4 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run45_3.child ⟨10454⟩ rfl orders45_4

def snapshot45 : Snapshot := run45_4.child

def node45 : ControlRoom.Node where
  snapshot := snapshot45
  title := "Outage recovered"
  summary := "Dispatch won the conflict. Failure returned the active reservation; repair and restart restored service. All fuel and the operator are home."
  outcome := .recovered
  candidates :=
    []
  candidateIdsUnique := by decide

def orders46_0 : OrderSet Workcell.Intent where
  orders := [ order 10450 10 0 (cancel .primary) ]
  idsUnique := by decide
def run46_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot2 ⟨10460⟩ rfl orders46_0

def orders46_1 : OrderSet Workcell.Intent where
  orders := [ order 10453 10 0 (leave .primary) ]
  idsUnique := by decide
def run46_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run46_0.child ⟨10461⟩ rfl orders46_1

def snapshot46 : Snapshot := run46_1.child

def node46 : ControlRoom.Node where
  snapshot := snapshot46
  title := "Resources protected"
  summary := "No fuel was spent. The operator returned safely, and 20 L remains available."
  outcome := .conserved
  candidates :=
    []
  candidateIdsUnique := by decide

def orders47_0 : OrderSet Workcell.Intent where
  orders := [ order 10460 10 0 (leave .primary) ]
  idsUnique := by decide
def run47_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot1 ⟨10470⟩ rfl orders47_0

def snapshot47 : Snapshot := run47_0.child

def node47 : ControlRoom.Node where
  snapshot := snapshot47
  title := "Resources protected"
  summary := "No fuel was spent. The operator returned safely, and 20 L remains available."
  outcome := .conserved
  candidates :=
    []
  candidateIdsUnique := by decide

def orders48_0 : OrderSet Workcell.Intent where
  orders := [ order 10470 10 0 (enter .secondary) ]
  idsUnique := by decide
def run48_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot0 ⟨10480⟩ rfl orders48_0

def snapshot48 : Snapshot := run48_0.child

def node48 : ControlRoom.Node where
  snapshot := snapshot48
  title := "Operator on station"
  summary := "Choose whether to release work. Your operator cannot staff both stations at once."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10480 (reserve .secondary), label := "Release the first lot", detail := "Reserve 10 L. Keep 10 L available for a second order." },
      { candidate := candidate 10930 (leave .secondary), label := "Stand down", detail := "Return the operator and preserve the full 20 L supply." },
      { candidate := candidate 90048 (enter .primary), label := "Staff both stations", detail := "Rejected: the unique operator is already in custody." } ]
  candidateIdsUnique := by decide

def orders49_0 : OrderSet Workcell.Intent where
  orders := [ order 10480 10 0 (reserve .secondary) ]
  idsUnique := by decide
def run49_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot48 ⟨10490⟩ rfl orders49_0

def snapshot49 : Snapshot := run49_0.child

def node49 : ControlRoom.Node where
  snapshot := snapshot49
  title := "First lot reserved"
  summary := "The first 10 L is reserved. Start the work or unwind the order."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10490 (dispatch .secondary), label := "Start production", detail := "Lock the operator and labor to the first lot." },
      { candidate := candidate 10910 (dispatch .secondary), label := "Dispatch", detail := "Both orders are valid now. Dispatch wins arbitration; departure loses. Inspect automatic failure and safe recovery." },
      { candidate := candidate 10911 (leave .secondary), label := "Leave", detail := "Both orders are valid now. Dispatch wins arbitration; departure loses. Inspect automatic failure and safe recovery." },
      { candidate := candidate 10920 (cancel .secondary), label := "Cancel the order", detail := "Return the reservation and release the operator." } ]
  candidateIdsUnique := by decide

def orders50_0 : OrderSet Workcell.Intent where
  orders := [ order 10490 10 0 (dispatch .secondary) ]
  idsUnique := by decide
def run50_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot49 ⟨10500⟩ rfl orders50_0

def snapshot50 : Snapshot := run50_0.child

def node50 : ControlRoom.Node where
  snapshot := snapshot50
  title := "Production is live"
  summary := "Labor and the operator are busy. Queue ahead for throughput, or finish one lot before committing more fuel."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10500 (reserve .secondary), label := "Build a production pipeline", detail := "Reserve the second lot while the first is active. All fuel will be committed." },
      { candidate := candidate 10660 (advance .secondary), label := "Finish the first lot", detail := "Complete one lot before deciding whether to accept another." },
      { candidate := candidate 90050 (leave .secondary), label := "Leave active work", detail := "Rejected: active custody keeps the operator with the running lot." } ]
  candidateIdsUnique := by decide

def orders51_0 : OrderSet Workcell.Intent where
  orders := [ order 10500 10 0 (reserve .secondary) ]
  idsUnique := by decide
def run51_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot50 ⟨10510⟩ rfl orders51_0

def snapshot51 : Snapshot := run51_0.child

def node51 : ControlRoom.Node where
  snapshot := snapshot51
  title := "Two lots, one labor slot"
  summary := "The pipeline is full: 10 L active and 10 L reserved. Complete the first order to return the shared labor."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10510 (advance .secondary), label := "Finish with backlog ready", detail := "The first lot moves to output. The next lot waits for labor." } ]
  candidateIdsUnique := by decide

def orders52_0 : OrderSet Workcell.Intent where
  orders := [ order 10510 10 0 (advance .secondary) ]
  idsUnique := by decide
def run52_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot51 ⟨10520⟩ rfl orders52_0

def orders52_1 : OrderSet Workcell.Intent where
  orders := [ order 10513 10 0 (complete .secondary) ]
  idsUnique := by decide
def run52_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run52_0.child ⟨10521⟩ rfl orders52_1

def snapshot52 : Snapshot := run52_1.child

def node52 : ControlRoom.Node where
  snapshot := snapshot52
  title := "The output bay is the bottleneck"
  summary := "Both bays contain work. Collect and dispatch together, or explore what happens when output remains blocked."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10520 (collect .secondary), label := "Collect", detail := "Deliver the first lot and start the second in one simultaneous order set." },
      { candidate := candidate 10521 (dispatch .secondary), label := "Dispatch", detail := "Deliver the first lot and start the second in one simultaneous order set." },
      { candidate := candidate 10570 (dispatch .secondary), label := "Dispatch into backpressure", detail := "Start the next lot before clearing output. You must free the bay before completion." },
      { candidate := candidate 10650 (cancel .secondary), label := "Cancel backlog and cash out", detail := "Return the second reservation and deliver the first lot." } ]
  candidateIdsUnique := by decide

def orders53_0 : OrderSet Workcell.Intent where
  orders := [ order 10520 10 0 (collect .secondary), order 10521 10 1 (dispatch .secondary) ]
  idsUnique := by decide
def run53_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot52 ⟨10530⟩ rfl orders53_0

def snapshot53 : Snapshot := run53_0.child

def node53 : ControlRoom.Node where
  snapshot := snapshot53
  title := "The final lot is running"
  summary := "The first lot has been delivered. Finish the second or cancel it safely before delivery."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10530 (advance .secondary), label := "Finish the second lot", detail := "Advance the final unit of work. The operator remains in active custody." },
      { candidate := candidate 10560 (cancelActive .secondary), label := "Cancel active work safely", detail := "Return the final lot’s fuel and labor, then release the operator." },
      { candidate := candidate 90053 (leave .secondary), label := "Leave active work", detail := "Rejected: active custody keeps the operator with the running lot." } ]
  candidateIdsUnique := by decide

def orders54_0 : OrderSet Workcell.Intent where
  orders := [ order 10530 10 0 (advance .secondary) ]
  idsUnique := by decide
def run54_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot53 ⟨10540⟩ rfl orders54_0

def snapshot54 : Snapshot := run54_0.child

def node54 : ControlRoom.Node where
  snapshot := snapshot54
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10540 (complete .secondary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders55_0 : OrderSet Workcell.Intent where
  orders := [ order 10540 10 0 (complete .secondary) ]
  idsUnique := by decide
def run55_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot54 ⟨10550⟩ rfl orders55_0

def snapshot55 : Snapshot := run55_0.child

def node55 : ControlRoom.Node where
  snapshot := snapshot55
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10550 (collect .secondary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10551 (leave .secondary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders56_0 : OrderSet Workcell.Intent where
  orders := [ order 10550 10 0 (collect .secondary), order 10551 10 1 (leave .secondary) ]
  idsUnique := by decide
def run56_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot55 ⟨10560⟩ rfl orders56_0

def snapshot56 : Snapshot := run56_0.child

def node56 : ControlRoom.Node where
  snapshot := snapshot56
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders57_0 : OrderSet Workcell.Intent where
  orders := [ order 10560 10 0 (cancelActive .secondary) ]
  idsUnique := by decide
def run57_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot53 ⟨10570⟩ rfl orders57_0

def orders57_1 : OrderSet Workcell.Intent where
  orders := [ order 10563 10 0 (leave .secondary) ]
  idsUnique := by decide
def run57_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run57_0.child ⟨10571⟩ rfl orders57_1

def snapshot57 : Snapshot := run57_1.child

def node57 : ControlRoom.Node where
  snapshot := snapshot57
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders58_0 : OrderSet Workcell.Intent where
  orders := [ order 10570 10 0 (dispatch .secondary) ]
  idsUnique := by decide
def run58_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot52 ⟨10580⟩ rfl orders58_0

def snapshot58 : Snapshot := run58_0.child

def node58 : ControlRoom.Node where
  snapshot := snapshot58
  title := "Work in progress. Output occupied."
  summary := "The second lot is active, but the first still occupies output. Choose when to clear it."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10580 (advance .secondary), label := "Work before clearing the bay", detail := "Finish the work, leaving the output bay occupied. Completion will be blocked." },
      { candidate := candidate 10620 (collect .secondary), label := "Collect", detail := "Clear the first lot and advance the second at the same logical tick." },
      { candidate := candidate 10621 (advance .secondary), label := "Advance", detail := "Clear the first lot and advance the second at the same logical tick." } ]
  candidateIdsUnique := by decide

def orders59_0 : OrderSet Workcell.Intent where
  orders := [ order 10580 10 0 (advance .secondary) ]
  idsUnique := by decide
def run59_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot58 ⟨10590⟩ rfl orders59_0

def snapshot59 : Snapshot := run59_0.child

def node59 : ControlRoom.Node where
  snapshot := snapshot59
  title := "Completion is blocked"
  summary := "Work is complete, but the one-slot output queue is full. Clear the first output before completing the next."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10590 (collect .secondary), label := "Clear the output bottleneck", detail := "Deliver the first lot to make room for the completed second lot." },
      { candidate := candidate 90059 (complete .secondary), label := "Complete into a full bay", detail := "Rejected: output capacity must be freed before completion." } ]
  candidateIdsUnique := by decide

def orders60_0 : OrderSet Workcell.Intent where
  orders := [ order 10590 10 0 (collect .secondary) ]
  idsUnique := by decide
def run60_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot59 ⟨10600⟩ rfl orders60_0

def snapshot60 : Snapshot := run60_0.child

def node60 : ControlRoom.Node where
  snapshot := snapshot60
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10600 (complete .secondary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders61_0 : OrderSet Workcell.Intent where
  orders := [ order 10600 10 0 (complete .secondary) ]
  idsUnique := by decide
def run61_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot60 ⟨10610⟩ rfl orders61_0

def snapshot61 : Snapshot := run61_0.child

def node61 : ControlRoom.Node where
  snapshot := snapshot61
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10610 (collect .secondary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10611 (leave .secondary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders62_0 : OrderSet Workcell.Intent where
  orders := [ order 10610 10 0 (collect .secondary), order 10611 10 1 (leave .secondary) ]
  idsUnique := by decide
def run62_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot61 ⟨10620⟩ rfl orders62_0

def snapshot62 : Snapshot := run62_0.child

def node62 : ControlRoom.Node where
  snapshot := snapshot62
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders63_0 : OrderSet Workcell.Intent where
  orders := [ order 10620 10 0 (collect .secondary), order 10621 10 1 (advance .secondary) ]
  idsUnique := by decide
def run63_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot58 ⟨10630⟩ rfl orders63_0

def snapshot63 : Snapshot := run63_0.child

def node63 : ControlRoom.Node where
  snapshot := snapshot63
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10630 (complete .secondary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders64_0 : OrderSet Workcell.Intent where
  orders := [ order 10630 10 0 (complete .secondary) ]
  idsUnique := by decide
def run64_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot63 ⟨10640⟩ rfl orders64_0

def snapshot64 : Snapshot := run64_0.child

def node64 : ControlRoom.Node where
  snapshot := snapshot64
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10640 (collect .secondary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10641 (leave .secondary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders65_0 : OrderSet Workcell.Intent where
  orders := [ order 10640 10 0 (collect .secondary), order 10641 10 1 (leave .secondary) ]
  idsUnique := by decide
def run65_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot64 ⟨10650⟩ rfl orders65_0

def snapshot65 : Snapshot := run65_0.child

def node65 : ControlRoom.Node where
  snapshot := snapshot65
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders66_0 : OrderSet Workcell.Intent where
  orders := [ order 10650 10 0 (cancel .secondary) ]
  idsUnique := by decide
def run66_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot52 ⟨10660⟩ rfl orders66_0

def orders66_1 : OrderSet Workcell.Intent where
  orders := [ order 10653 10 0 (collect .secondary) ]
  idsUnique := by decide
def run66_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run66_0.child ⟨10661⟩ rfl orders66_1

def orders66_2 : OrderSet Workcell.Intent where
  orders := [ order 10656 10 0 (leave .secondary) ]
  idsUnique := by decide
def run66_2 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run66_1.child ⟨10662⟩ rfl orders66_2

def snapshot66 : Snapshot := run66_2.child

def node66 : ControlRoom.Node where
  snapshot := snapshot66
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders67_0 : OrderSet Workcell.Intent where
  orders := [ order 10660 10 0 (advance .secondary) ]
  idsUnique := by decide
def run67_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot50 ⟨10670⟩ rfl orders67_0

def orders67_1 : OrderSet Workcell.Intent where
  orders := [ order 10663 10 0 (complete .secondary) ]
  idsUnique := by decide
def run67_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run67_0.child ⟨10671⟩ rfl orders67_1

def snapshot67 : Snapshot := run67_1.child

def node67 : ControlRoom.Node where
  snapshot := snapshot67
  title := "The first lot is ready"
  summary := "The first lot occupies output custody. Deliver it now or reserve your remaining supply."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10670 (collect .secondary), label := "Deliver the first order", detail := "Collect 10 L and two service credits. Keep the operator for a possible second lot." },
      { candidate := candidate 10760 (reserve .secondary), label := "Queue before collection", detail := "Commit the remaining fuel while the first output still occupies its bay." } ]
  candidateIdsUnique := by decide

def orders68_0 : OrderSet Workcell.Intent where
  orders := [ order 10670 10 0 (collect .secondary) ]
  idsUnique := by decide
def run68_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot67 ⟨10680⟩ rfl orders68_0

def snapshot68 : Snapshot := run68_0.child

def node68 : ControlRoom.Node where
  snapshot := snapshot68
  title := "Bank the shift or keep going?"
  summary := "You have delivered 10 L. Finish conservatively, or commit the remaining fuel to double production."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10680 (leave .secondary), label := "Close the shift at 10 L", detail := "Bank two credits and return the operator. Preserve 10 L for tomorrow." },
      { candidate := candidate 10690 (reserve .secondary), label := "Take the second order", detail := "Commit the remaining 10 L for another two service credits." } ]
  candidateIdsUnique := by decide

def orders69_0 : OrderSet Workcell.Intent where
  orders := [ order 10680 10 0 (leave .secondary) ]
  idsUnique := by decide
def run69_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot68 ⟨10690⟩ rfl orders69_0

def snapshot69 : Snapshot := run69_0.child

def node69 : ControlRoom.Node where
  snapshot := snapshot69
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders70_0 : OrderSet Workcell.Intent where
  orders := [ order 10690 10 0 (reserve .secondary) ]
  idsUnique := by decide
def run70_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot68 ⟨10700⟩ rfl orders70_0

def snapshot70 : Snapshot := run70_0.child

def node70 : ControlRoom.Node where
  snapshot := snapshot70
  title := "One last order"
  summary := "The second lot is funded. Dispatch it or preserve the reserve."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10700 (dispatch .secondary), label := "Run the second lot", detail := "Use the returned labor for the final order." },
      { candidate := candidate 10750 (cancel .secondary), label := "Protect the reserve", detail := "Cancel the second lot, return its fuel, and close with 10 L delivered." } ]
  candidateIdsUnique := by decide

def orders71_0 : OrderSet Workcell.Intent where
  orders := [ order 10700 10 0 (dispatch .secondary) ]
  idsUnique := by decide
def run71_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot70 ⟨10710⟩ rfl orders71_0

def snapshot71 : Snapshot := run71_0.child

def node71 : ControlRoom.Node where
  snapshot := snapshot71
  title := "The final lot is running"
  summary := "The first lot has been delivered. Finish the second or cancel it safely before delivery."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10710 (advance .secondary), label := "Finish the second lot", detail := "Advance the final unit of work. The operator remains in active custody." },
      { candidate := candidate 10740 (cancelActive .secondary), label := "Cancel active work safely", detail := "Return the final lot’s fuel and labor, then release the operator." },
      { candidate := candidate 90071 (leave .secondary), label := "Leave active work", detail := "Rejected: active custody keeps the operator with the running lot." } ]
  candidateIdsUnique := by decide

def orders72_0 : OrderSet Workcell.Intent where
  orders := [ order 10710 10 0 (advance .secondary) ]
  idsUnique := by decide
def run72_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot71 ⟨10720⟩ rfl orders72_0

def snapshot72 : Snapshot := run72_0.child

def node72 : ControlRoom.Node where
  snapshot := snapshot72
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10720 (complete .secondary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders73_0 : OrderSet Workcell.Intent where
  orders := [ order 10720 10 0 (complete .secondary) ]
  idsUnique := by decide
def run73_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot72 ⟨10730⟩ rfl orders73_0

def snapshot73 : Snapshot := run73_0.child

def node73 : ControlRoom.Node where
  snapshot := snapshot73
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10730 (collect .secondary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10731 (leave .secondary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders74_0 : OrderSet Workcell.Intent where
  orders := [ order 10730 10 0 (collect .secondary), order 10731 10 1 (leave .secondary) ]
  idsUnique := by decide
def run74_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot73 ⟨10740⟩ rfl orders74_0

def snapshot74 : Snapshot := run74_0.child

def node74 : ControlRoom.Node where
  snapshot := snapshot74
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders75_0 : OrderSet Workcell.Intent where
  orders := [ order 10740 10 0 (cancelActive .secondary) ]
  idsUnique := by decide
def run75_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot71 ⟨10750⟩ rfl orders75_0

def orders75_1 : OrderSet Workcell.Intent where
  orders := [ order 10743 10 0 (leave .secondary) ]
  idsUnique := by decide
def run75_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run75_0.child ⟨10751⟩ rfl orders75_1

def snapshot75 : Snapshot := run75_1.child

def node75 : ControlRoom.Node where
  snapshot := snapshot75
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders76_0 : OrderSet Workcell.Intent where
  orders := [ order 10750 10 0 (cancel .secondary) ]
  idsUnique := by decide
def run76_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot70 ⟨10760⟩ rfl orders76_0

def orders76_1 : OrderSet Workcell.Intent where
  orders := [ order 10753 10 0 (leave .secondary) ]
  idsUnique := by decide
def run76_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run76_0.child ⟨10761⟩ rfl orders76_1

def snapshot76 : Snapshot := run76_1.child

def node76 : ControlRoom.Node where
  snapshot := snapshot76
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders77_0 : OrderSet Workcell.Intent where
  orders := [ order 10760 10 0 (reserve .secondary) ]
  idsUnique := by decide
def run77_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot67 ⟨10770⟩ rfl orders77_0

def snapshot77 : Snapshot := run77_0.child

def node77 : ControlRoom.Node where
  snapshot := snapshot77
  title := "The output bay is the bottleneck"
  summary := "Both bays contain work. Collect and dispatch together, or explore what happens when output remains blocked."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10770 (collect .secondary), label := "Collect", detail := "Deliver the first lot and start the second in one simultaneous order set." },
      { candidate := candidate 10771 (dispatch .secondary), label := "Dispatch", detail := "Deliver the first lot and start the second in one simultaneous order set." },
      { candidate := candidate 10820 (dispatch .secondary), label := "Dispatch into backpressure", detail := "Start the next lot before clearing output. You must free the bay before completion." },
      { candidate := candidate 10900 (cancel .secondary), label := "Cancel backlog and cash out", detail := "Return the second reservation and deliver the first lot." } ]
  candidateIdsUnique := by decide

def orders78_0 : OrderSet Workcell.Intent where
  orders := [ order 10770 10 0 (collect .secondary), order 10771 10 1 (dispatch .secondary) ]
  idsUnique := by decide
def run78_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot77 ⟨10780⟩ rfl orders78_0

def snapshot78 : Snapshot := run78_0.child

def node78 : ControlRoom.Node where
  snapshot := snapshot78
  title := "The final lot is running"
  summary := "The first lot has been delivered. Finish the second or cancel it safely before delivery."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10780 (advance .secondary), label := "Finish the second lot", detail := "Advance the final unit of work. The operator remains in active custody." },
      { candidate := candidate 10810 (cancelActive .secondary), label := "Cancel active work safely", detail := "Return the final lot’s fuel and labor, then release the operator." },
      { candidate := candidate 90078 (leave .secondary), label := "Leave active work", detail := "Rejected: active custody keeps the operator with the running lot." } ]
  candidateIdsUnique := by decide

def orders79_0 : OrderSet Workcell.Intent where
  orders := [ order 10780 10 0 (advance .secondary) ]
  idsUnique := by decide
def run79_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot78 ⟨10790⟩ rfl orders79_0

def snapshot79 : Snapshot := run79_0.child

def node79 : ControlRoom.Node where
  snapshot := snapshot79
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10790 (complete .secondary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders80_0 : OrderSet Workcell.Intent where
  orders := [ order 10790 10 0 (complete .secondary) ]
  idsUnique := by decide
def run80_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot79 ⟨10800⟩ rfl orders80_0

def snapshot80 : Snapshot := run80_0.child

def node80 : ControlRoom.Node where
  snapshot := snapshot80
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10800 (collect .secondary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10801 (leave .secondary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders81_0 : OrderSet Workcell.Intent where
  orders := [ order 10800 10 0 (collect .secondary), order 10801 10 1 (leave .secondary) ]
  idsUnique := by decide
def run81_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot80 ⟨10810⟩ rfl orders81_0

def snapshot81 : Snapshot := run81_0.child

def node81 : ControlRoom.Node where
  snapshot := snapshot81
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders82_0 : OrderSet Workcell.Intent where
  orders := [ order 10810 10 0 (cancelActive .secondary) ]
  idsUnique := by decide
def run82_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot78 ⟨10820⟩ rfl orders82_0

def orders82_1 : OrderSet Workcell.Intent where
  orders := [ order 10813 10 0 (leave .secondary) ]
  idsUnique := by decide
def run82_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run82_0.child ⟨10821⟩ rfl orders82_1

def snapshot82 : Snapshot := run82_1.child

def node82 : ControlRoom.Node where
  snapshot := snapshot82
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders83_0 : OrderSet Workcell.Intent where
  orders := [ order 10820 10 0 (dispatch .secondary) ]
  idsUnique := by decide
def run83_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot77 ⟨10830⟩ rfl orders83_0

def snapshot83 : Snapshot := run83_0.child

def node83 : ControlRoom.Node where
  snapshot := snapshot83
  title := "Work in progress. Output occupied."
  summary := "The second lot is active, but the first still occupies output. Choose when to clear it."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10830 (advance .secondary), label := "Work before clearing the bay", detail := "Finish the work, leaving the output bay occupied. Completion will be blocked." },
      { candidate := candidate 10870 (collect .secondary), label := "Collect", detail := "Clear the first lot and advance the second at the same logical tick." },
      { candidate := candidate 10871 (advance .secondary), label := "Advance", detail := "Clear the first lot and advance the second at the same logical tick." } ]
  candidateIdsUnique := by decide

def orders84_0 : OrderSet Workcell.Intent where
  orders := [ order 10830 10 0 (advance .secondary) ]
  idsUnique := by decide
def run84_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot83 ⟨10840⟩ rfl orders84_0

def snapshot84 : Snapshot := run84_0.child

def node84 : ControlRoom.Node where
  snapshot := snapshot84
  title := "Completion is blocked"
  summary := "Work is complete, but the one-slot output queue is full. Clear the first output before completing the next."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10840 (collect .secondary), label := "Clear the output bottleneck", detail := "Deliver the first lot to make room for the completed second lot." },
      { candidate := candidate 90084 (complete .secondary), label := "Complete into a full bay", detail := "Rejected: output capacity must be freed before completion." } ]
  candidateIdsUnique := by decide

def orders85_0 : OrderSet Workcell.Intent where
  orders := [ order 10840 10 0 (collect .secondary) ]
  idsUnique := by decide
def run85_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot84 ⟨10850⟩ rfl orders85_0

def snapshot85 : Snapshot := run85_0.child

def node85 : ControlRoom.Node where
  snapshot := snapshot85
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10850 (complete .secondary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders86_0 : OrderSet Workcell.Intent where
  orders := [ order 10850 10 0 (complete .secondary) ]
  idsUnique := by decide
def run86_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot85 ⟨10860⟩ rfl orders86_0

def snapshot86 : Snapshot := run86_0.child

def node86 : ControlRoom.Node where
  snapshot := snapshot86
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10860 (collect .secondary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10861 (leave .secondary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders87_0 : OrderSet Workcell.Intent where
  orders := [ order 10860 10 0 (collect .secondary), order 10861 10 1 (leave .secondary) ]
  idsUnique := by decide
def run87_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot86 ⟨10870⟩ rfl orders87_0

def snapshot87 : Snapshot := run87_0.child

def node87 : ControlRoom.Node where
  snapshot := snapshot87
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders88_0 : OrderSet Workcell.Intent where
  orders := [ order 10870 10 0 (collect .secondary), order 10871 10 1 (advance .secondary) ]
  idsUnique := by decide
def run88_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot83 ⟨10880⟩ rfl orders88_0

def snapshot88 : Snapshot := run88_0.child

def node88 : ControlRoom.Node where
  snapshot := snapshot88
  title := "Final work complete"
  summary := "Work is complete and output capacity is available. Complete the transformation."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10880 (complete .secondary), label := "Complete the final order", detail := "Release labor and place the last output in the collection bay." } ]
  candidateIdsUnique := by decide

def orders89_0 : OrderSet Workcell.Intent where
  orders := [ order 10880 10 0 (complete .secondary) ]
  idsUnique := by decide
def run89_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot88 ⟨10890⟩ rfl orders89_0

def snapshot89 : Snapshot := run89_0.child

def node89 : ControlRoom.Node where
  snapshot := snapshot89
  title := "Ready to deliver"
  summary := "The second lot is in output custody. Deliver it and return the operator to close the shift."
  outcome := .active
  candidates :=
    [ { candidate := candidate 10890 (collect .secondary), label := "Collect", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." },
      { candidate := candidate 10891 (leave .secondary), label := "Leave", detail := "Finish the full 20 L shift, collect four credits, and return the operator together." } ]
  candidateIdsUnique := by decide

def orders90_0 : OrderSet Workcell.Intent where
  orders := [ order 10890 10 0 (collect .secondary), order 10891 10 1 (leave .secondary) ]
  idsUnique := by decide
def run90_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot89 ⟨10900⟩ rfl orders90_0

def snapshot90 : Snapshot := run90_0.child

def node90 : ControlRoom.Node where
  snapshot := snapshot90
  title := "Full shift delivered"
  summary := "20 L delivered. Four service credits. No stranded work. The operator and labor are safely returned."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders91_0 : OrderSet Workcell.Intent where
  orders := [ order 10900 10 0 (cancel .secondary) ]
  idsUnique := by decide
def run91_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot77 ⟨10910⟩ rfl orders91_0

def orders91_1 : OrderSet Workcell.Intent where
  orders := [ order 10903 10 0 (collect .secondary) ]
  idsUnique := by decide
def run91_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run91_0.child ⟨10911⟩ rfl orders91_1

def orders91_2 : OrderSet Workcell.Intent where
  orders := [ order 10906 10 0 (leave .secondary) ]
  idsUnique := by decide
def run91_2 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run91_1.child ⟨10912⟩ rfl orders91_2

def snapshot91 : Snapshot := run91_2.child

def node91 : ControlRoom.Node where
  snapshot := snapshot91
  title := "A measured shift"
  summary := "10 L delivered, two credits collected, and 10 L preserved. The operator is home."
  outcome := .productive
  candidates :=
    []
  candidateIdsUnique := by decide

def orders92_0 : OrderSet Workcell.Intent where
  orders := [ order 10910 10 0 (dispatch .secondary), order 10911 10 1 (leave .secondary) ]
  idsUnique := by decide
def run92_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot49 ⟨10920⟩ rfl orders92_0

def orders92_1 : OrderSet Workcell.Intent where
  orders := [ order 10913 10 0 (fail .secondary) ]
  idsUnique := by decide
def run92_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run92_0.child ⟨10921⟩ rfl orders92_1

def orders92_2 : OrderSet Workcell.Intent where
  orders := [ order 10916 10 0 (repair .secondary) ]
  idsUnique := by decide
def run92_2 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run92_1.child ⟨10922⟩ rfl orders92_2

def orders92_3 : OrderSet Workcell.Intent where
  orders := [ order 10919 10 0 (start .secondary) ]
  idsUnique := by decide
def run92_3 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run92_2.child ⟨10923⟩ rfl orders92_3

def orders92_4 : OrderSet Workcell.Intent where
  orders := [ order 10922 10 0 (leave .secondary) ]
  idsUnique := by decide
def run92_4 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run92_3.child ⟨10924⟩ rfl orders92_4

def snapshot92 : Snapshot := run92_4.child

def node92 : ControlRoom.Node where
  snapshot := snapshot92
  title := "Outage recovered"
  summary := "Dispatch won the conflict. Failure returned the active reservation; repair and restart restored service. All fuel and the operator are home."
  outcome := .recovered
  candidates :=
    []
  candidateIdsUnique := by decide

def orders93_0 : OrderSet Workcell.Intent where
  orders := [ order 10920 10 0 (cancel .secondary) ]
  idsUnique := by decide
def run93_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot49 ⟨10930⟩ rfl orders93_0

def orders93_1 : OrderSet Workcell.Intent where
  orders := [ order 10923 10 0 (leave .secondary) ]
  idsUnique := by decide
def run93_1 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  run93_0.child ⟨10931⟩ rfl orders93_1

def snapshot93 : Snapshot := run93_1.child

def node93 : ControlRoom.Node where
  snapshot := snapshot93
  title := "Resources protected"
  summary := "No fuel was spent. The operator returned safely, and 20 L remains available."
  outcome := .conserved
  candidates :=
    []
  candidateIdsUnique := by decide

def orders94_0 : OrderSet Workcell.Intent where
  orders := [ order 10930 10 0 (leave .secondary) ]
  idsUnique := by decide
def run94_0 := resolveSnapshotOrderSet Workcell.executor ControlRoom.initialState
  snapshot48 ⟨10940⟩ rfl orders94_0

def snapshot94 : Snapshot := run94_0.child

def node94 : ControlRoom.Node where
  snapshot := snapshot94
  title := "Resources protected"
  summary := "No fuel was spent. The operator returned safely, and 20 L remains available."
  outcome := .conserved
  candidates :=
    []
  candidateIdsUnique := by decide

def nodes : List ControlRoom.Node := [ node0, node1, node2, node3, node4, node5, node6, node7, node8, node9, node10, node11, node12, node13, node14, node15, node16, node17, node18, node19, node20, node21, node22, node23, node24, node25, node26, node27, node28, node29, node30, node31, node32, node33, node34, node35, node36, node37, node38, node39, node40, node41, node42, node43, node44, node45, node46, node47, node48, node49, node50, node51, node52, node53, node54, node55, node56, node57, node58, node59, node60, node61, node62, node63, node64, node65, node66, node67, node68, node69, node70, node71, node72, node73, node74, node75, node76, node77, node78, node79, node80, node81, node82, node83, node84, node85, node86, node87, node88, node89, node90, node91, node92, node93, node94 ]
def provedNodes := nodes.map ControlRoom.provedNode
abbrev ProvedResolution := Maquina.CommandGraphResolution Workcell.executor ControlRoom.initialState provedNodes
structure Resolution where
  proof : ProvedResolution
  label : String
  summary : String
  automaticOrders : List String

def resolution0 : Resolution where
  proof :=
    { id := 0
      source := ControlRoom.provedNode node0
      target := ControlRoom.provedNode node1
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10000⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot0 run1_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Staff Primary"
  summary := "Give the primary service line the shift’s only operator."
  automaticOrders := [  ]

def resolution1 : Resolution where
  proof :=
    { id := 1
      source := ControlRoom.provedNode node1
      target := ControlRoom.provedNode node2
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10010⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot1 run2_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Release the first lot"
  summary := "Reserve 10 L. Keep 10 L available for a second order."
  automaticOrders := [  ]

def resolution2 : Resolution where
  proof :=
    { id := 2
      source := ControlRoom.provedNode node2
      target := ControlRoom.provedNode node3
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10020⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot2 run3_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Start production"
  summary := "Lock the operator and labor to the first lot."
  automaticOrders := [  ]

def resolution3 : Resolution where
  proof :=
    { id := 3
      source := ControlRoom.provedNode node3
      target := ControlRoom.provedNode node4
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10030⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot3 run4_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Build a production pipeline"
  summary := "Reserve the second lot while the first is active. All fuel will be committed."
  automaticOrders := [  ]

def resolution4 : Resolution where
  proof :=
    { id := 4
      source := ControlRoom.provedNode node4
      target := ControlRoom.provedNode node5
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10040⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot4 run5_0, provedTick run5_0.child run5_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Finish with backlog ready"
  summary := "The first lot moves to output. The next lot waits for labor."
  automaticOrders := [ "complete" ]

def resolution5 : Resolution where
  proof :=
    { id := 5
      source := ControlRoom.provedNode node5
      target := ControlRoom.provedNode node6
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10050⟩, ⟨10051⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot5 run6_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Clear output + dispatch"
  summary := "Deliver the first lot and start the second in one simultaneous order set."
  automaticOrders := [  ]

def resolution6 : Resolution where
  proof :=
    { id := 6
      source := ControlRoom.provedNode node6
      target := ControlRoom.provedNode node7
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10060⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot6 run7_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Finish the second lot"
  summary := "Advance the final unit of work. The operator remains in active custody."
  automaticOrders := [  ]

def resolution7 : Resolution where
  proof :=
    { id := 7
      source := ControlRoom.provedNode node7
      target := ControlRoom.provedNode node8
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10070⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot7 run8_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution8 : Resolution where
  proof :=
    { id := 8
      source := ControlRoom.provedNode node8
      target := ControlRoom.provedNode node9
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10080⟩, ⟨10081⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot8 run9_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution9 : Resolution where
  proof :=
    { id := 9
      source := ControlRoom.provedNode node6
      target := ControlRoom.provedNode node10
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10090⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot6 run10_0, provedTick run10_0.child run10_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Cancel active work safely"
  summary := "Return the final lot’s fuel and labor, then release the operator."
  automaticOrders := [ "leave" ]

def resolution10 : Resolution where
  proof :=
    { id := 10
      source := ControlRoom.provedNode node5
      target := ControlRoom.provedNode node11
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10100⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot5 run11_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Dispatch into backpressure"
  summary := "Start the next lot before clearing output. You must free the bay before completion."
  automaticOrders := [  ]

def resolution11 : Resolution where
  proof :=
    { id := 11
      source := ControlRoom.provedNode node11
      target := ControlRoom.provedNode node12
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10110⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot11 run12_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Work before clearing the bay"
  summary := "Finish the work, leaving the output bay occupied. Completion will be blocked."
  automaticOrders := [  ]

def resolution12 : Resolution where
  proof :=
    { id := 12
      source := ControlRoom.provedNode node12
      target := ControlRoom.provedNode node13
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10120⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot12 run13_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Clear the output bottleneck"
  summary := "Deliver the first lot to make room for the completed second lot."
  automaticOrders := [  ]

def resolution13 : Resolution where
  proof :=
    { id := 13
      source := ControlRoom.provedNode node13
      target := ControlRoom.provedNode node14
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10130⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot13 run14_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution14 : Resolution where
  proof :=
    { id := 14
      source := ControlRoom.provedNode node14
      target := ControlRoom.provedNode node15
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10140⟩, ⟨10141⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot14 run15_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution15 : Resolution where
  proof :=
    { id := 15
      source := ControlRoom.provedNode node11
      target := ControlRoom.provedNode node16
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10150⟩, ⟨10151⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot11 run16_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Collect + advance"
  summary := "Clear the first lot and advance the second at the same logical tick."
  automaticOrders := [  ]

def resolution16 : Resolution where
  proof :=
    { id := 16
      source := ControlRoom.provedNode node16
      target := ControlRoom.provedNode node17
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10160⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot16 run17_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution17 : Resolution where
  proof :=
    { id := 17
      source := ControlRoom.provedNode node17
      target := ControlRoom.provedNode node18
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10170⟩, ⟨10171⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot17 run18_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution18 : Resolution where
  proof :=
    { id := 18
      source := ControlRoom.provedNode node5
      target := ControlRoom.provedNode node19
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10180⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot5 run19_0, provedTick run19_0.child run19_1, provedTick run19_1.child run19_2 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩⟩ }
  label := "Cancel backlog and cash out"
  summary := "Return the second reservation and deliver the first lot."
  automaticOrders := [ "collect", "leave" ]

def resolution19 : Resolution where
  proof :=
    { id := 19
      source := ControlRoom.provedNode node3
      target := ControlRoom.provedNode node20
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10190⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot3 run20_0, provedTick run20_0.child run20_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Finish the first lot"
  summary := "Complete one lot before deciding whether to accept another."
  automaticOrders := [ "complete" ]

def resolution20 : Resolution where
  proof :=
    { id := 20
      source := ControlRoom.provedNode node20
      target := ControlRoom.provedNode node21
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10200⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot20 run21_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver the first order"
  summary := "Collect 10 L and two service credits. Keep the operator for a possible second lot."
  automaticOrders := [  ]

def resolution21 : Resolution where
  proof :=
    { id := 21
      source := ControlRoom.provedNode node21
      target := ControlRoom.provedNode node22
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10210⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot21 run22_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Close the shift at 10 L"
  summary := "Bank two credits and return the operator. Preserve 10 L for tomorrow."
  automaticOrders := [  ]

def resolution22 : Resolution where
  proof :=
    { id := 22
      source := ControlRoom.provedNode node21
      target := ControlRoom.provedNode node23
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10220⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot21 run23_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Take the second order"
  summary := "Commit the remaining 10 L for another two service credits."
  automaticOrders := [  ]

def resolution23 : Resolution where
  proof :=
    { id := 23
      source := ControlRoom.provedNode node23
      target := ControlRoom.provedNode node24
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10230⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot23 run24_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Run the second lot"
  summary := "Use the returned labor for the final order."
  automaticOrders := [  ]

def resolution24 : Resolution where
  proof :=
    { id := 24
      source := ControlRoom.provedNode node24
      target := ControlRoom.provedNode node25
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10240⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot24 run25_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Finish the second lot"
  summary := "Advance the final unit of work. The operator remains in active custody."
  automaticOrders := [  ]

def resolution25 : Resolution where
  proof :=
    { id := 25
      source := ControlRoom.provedNode node25
      target := ControlRoom.provedNode node26
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10250⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot25 run26_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution26 : Resolution where
  proof :=
    { id := 26
      source := ControlRoom.provedNode node26
      target := ControlRoom.provedNode node27
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10260⟩, ⟨10261⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot26 run27_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution27 : Resolution where
  proof :=
    { id := 27
      source := ControlRoom.provedNode node24
      target := ControlRoom.provedNode node28
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10270⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot24 run28_0, provedTick run28_0.child run28_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Cancel active work safely"
  summary := "Return the final lot’s fuel and labor, then release the operator."
  automaticOrders := [ "leave" ]

def resolution28 : Resolution where
  proof :=
    { id := 28
      source := ControlRoom.provedNode node23
      target := ControlRoom.provedNode node29
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10280⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot23 run29_0, provedTick run29_0.child run29_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Protect the reserve"
  summary := "Cancel the second lot, return its fuel, and close with 10 L delivered."
  automaticOrders := [ "leave" ]

def resolution29 : Resolution where
  proof :=
    { id := 29
      source := ControlRoom.provedNode node20
      target := ControlRoom.provedNode node30
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10290⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot20 run30_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Queue before collection"
  summary := "Commit the remaining fuel while the first output still occupies its bay."
  automaticOrders := [  ]

def resolution30 : Resolution where
  proof :=
    { id := 30
      source := ControlRoom.provedNode node30
      target := ControlRoom.provedNode node31
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10300⟩, ⟨10301⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot30 run31_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Clear output + dispatch"
  summary := "Deliver the first lot and start the second in one simultaneous order set."
  automaticOrders := [  ]

def resolution31 : Resolution where
  proof :=
    { id := 31
      source := ControlRoom.provedNode node31
      target := ControlRoom.provedNode node32
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10310⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot31 run32_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Finish the second lot"
  summary := "Advance the final unit of work. The operator remains in active custody."
  automaticOrders := [  ]

def resolution32 : Resolution where
  proof :=
    { id := 32
      source := ControlRoom.provedNode node32
      target := ControlRoom.provedNode node33
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10320⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot32 run33_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution33 : Resolution where
  proof :=
    { id := 33
      source := ControlRoom.provedNode node33
      target := ControlRoom.provedNode node34
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10330⟩, ⟨10331⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot33 run34_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution34 : Resolution where
  proof :=
    { id := 34
      source := ControlRoom.provedNode node31
      target := ControlRoom.provedNode node35
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10340⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot31 run35_0, provedTick run35_0.child run35_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Cancel active work safely"
  summary := "Return the final lot’s fuel and labor, then release the operator."
  automaticOrders := [ "leave" ]

def resolution35 : Resolution where
  proof :=
    { id := 35
      source := ControlRoom.provedNode node30
      target := ControlRoom.provedNode node36
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10350⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot30 run36_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Dispatch into backpressure"
  summary := "Start the next lot before clearing output. You must free the bay before completion."
  automaticOrders := [  ]

def resolution36 : Resolution where
  proof :=
    { id := 36
      source := ControlRoom.provedNode node36
      target := ControlRoom.provedNode node37
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10360⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot36 run37_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Work before clearing the bay"
  summary := "Finish the work, leaving the output bay occupied. Completion will be blocked."
  automaticOrders := [  ]

def resolution37 : Resolution where
  proof :=
    { id := 37
      source := ControlRoom.provedNode node37
      target := ControlRoom.provedNode node38
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10370⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot37 run38_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Clear the output bottleneck"
  summary := "Deliver the first lot to make room for the completed second lot."
  automaticOrders := [  ]

def resolution38 : Resolution where
  proof :=
    { id := 38
      source := ControlRoom.provedNode node38
      target := ControlRoom.provedNode node39
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10380⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot38 run39_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution39 : Resolution where
  proof :=
    { id := 39
      source := ControlRoom.provedNode node39
      target := ControlRoom.provedNode node40
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10390⟩, ⟨10391⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot39 run40_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution40 : Resolution where
  proof :=
    { id := 40
      source := ControlRoom.provedNode node36
      target := ControlRoom.provedNode node41
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10400⟩, ⟨10401⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot36 run41_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Collect + advance"
  summary := "Clear the first lot and advance the second at the same logical tick."
  automaticOrders := [  ]

def resolution41 : Resolution where
  proof :=
    { id := 41
      source := ControlRoom.provedNode node41
      target := ControlRoom.provedNode node42
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10410⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot41 run42_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution42 : Resolution where
  proof :=
    { id := 42
      source := ControlRoom.provedNode node42
      target := ControlRoom.provedNode node43
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10420⟩, ⟨10421⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot42 run43_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution43 : Resolution where
  proof :=
    { id := 43
      source := ControlRoom.provedNode node30
      target := ControlRoom.provedNode node44
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10430⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot30 run44_0, provedTick run44_0.child run44_1, provedTick run44_1.child run44_2 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩⟩ }
  label := "Cancel backlog and cash out"
  summary := "Return the second reservation and deliver the first lot."
  automaticOrders := [ "collect", "leave" ]

def resolution44 : Resolution where
  proof :=
    { id := 44
      source := ControlRoom.provedNode node2
      target := ControlRoom.provedNode node45
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10440⟩, ⟨10441⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot2 run45_0, provedTick run45_0.child run45_1, provedTick run45_1.child run45_2, provedTick run45_2.child run45_3, provedTick run45_3.child run45_4 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩⟩⟩⟩ }
  label := "Dispatch + depart"
  summary := "Both orders are valid now. Dispatch wins arbitration; departure loses. Inspect automatic failure and safe recovery."
  automaticOrders := [ "fail", "repair", "start", "leave" ]

def resolution45 : Resolution where
  proof :=
    { id := 45
      source := ControlRoom.provedNode node2
      target := ControlRoom.provedNode node46
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10450⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot2 run46_0, provedTick run46_0.child run46_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Cancel the order"
  summary := "Return the reservation and release the operator."
  automaticOrders := [ "leave" ]

def resolution46 : Resolution where
  proof :=
    { id := 46
      source := ControlRoom.provedNode node1
      target := ControlRoom.provedNode node47
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10460⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot1 run47_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Stand down"
  summary := "Return the operator and preserve the full 20 L supply."
  automaticOrders := [  ]

def resolution47 : Resolution where
  proof :=
    { id := 47
      source := ControlRoom.provedNode node0
      target := ControlRoom.provedNode node48
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10470⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot0 run48_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Staff Secondary"
  summary := "Give the secondary service line the shift’s only operator."
  automaticOrders := [  ]

def resolution48 : Resolution where
  proof :=
    { id := 48
      source := ControlRoom.provedNode node48
      target := ControlRoom.provedNode node49
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10480⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot48 run49_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Release the first lot"
  summary := "Reserve 10 L. Keep 10 L available for a second order."
  automaticOrders := [  ]

def resolution49 : Resolution where
  proof :=
    { id := 49
      source := ControlRoom.provedNode node49
      target := ControlRoom.provedNode node50
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10490⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot49 run50_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Start production"
  summary := "Lock the operator and labor to the first lot."
  automaticOrders := [  ]

def resolution50 : Resolution where
  proof :=
    { id := 50
      source := ControlRoom.provedNode node50
      target := ControlRoom.provedNode node51
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10500⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot50 run51_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Build a production pipeline"
  summary := "Reserve the second lot while the first is active. All fuel will be committed."
  automaticOrders := [  ]

def resolution51 : Resolution where
  proof :=
    { id := 51
      source := ControlRoom.provedNode node51
      target := ControlRoom.provedNode node52
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10510⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot51 run52_0, provedTick run52_0.child run52_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Finish with backlog ready"
  summary := "The first lot moves to output. The next lot waits for labor."
  automaticOrders := [ "complete" ]

def resolution52 : Resolution where
  proof :=
    { id := 52
      source := ControlRoom.provedNode node52
      target := ControlRoom.provedNode node53
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10520⟩, ⟨10521⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot52 run53_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Clear output + dispatch"
  summary := "Deliver the first lot and start the second in one simultaneous order set."
  automaticOrders := [  ]

def resolution53 : Resolution where
  proof :=
    { id := 53
      source := ControlRoom.provedNode node53
      target := ControlRoom.provedNode node54
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10530⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot53 run54_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Finish the second lot"
  summary := "Advance the final unit of work. The operator remains in active custody."
  automaticOrders := [  ]

def resolution54 : Resolution where
  proof :=
    { id := 54
      source := ControlRoom.provedNode node54
      target := ControlRoom.provedNode node55
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10540⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot54 run55_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution55 : Resolution where
  proof :=
    { id := 55
      source := ControlRoom.provedNode node55
      target := ControlRoom.provedNode node56
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10550⟩, ⟨10551⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot55 run56_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution56 : Resolution where
  proof :=
    { id := 56
      source := ControlRoom.provedNode node53
      target := ControlRoom.provedNode node57
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10560⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot53 run57_0, provedTick run57_0.child run57_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Cancel active work safely"
  summary := "Return the final lot’s fuel and labor, then release the operator."
  automaticOrders := [ "leave" ]

def resolution57 : Resolution where
  proof :=
    { id := 57
      source := ControlRoom.provedNode node52
      target := ControlRoom.provedNode node58
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10570⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot52 run58_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Dispatch into backpressure"
  summary := "Start the next lot before clearing output. You must free the bay before completion."
  automaticOrders := [  ]

def resolution58 : Resolution where
  proof :=
    { id := 58
      source := ControlRoom.provedNode node58
      target := ControlRoom.provedNode node59
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10580⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot58 run59_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Work before clearing the bay"
  summary := "Finish the work, leaving the output bay occupied. Completion will be blocked."
  automaticOrders := [  ]

def resolution59 : Resolution where
  proof :=
    { id := 59
      source := ControlRoom.provedNode node59
      target := ControlRoom.provedNode node60
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10590⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot59 run60_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Clear the output bottleneck"
  summary := "Deliver the first lot to make room for the completed second lot."
  automaticOrders := [  ]

def resolution60 : Resolution where
  proof :=
    { id := 60
      source := ControlRoom.provedNode node60
      target := ControlRoom.provedNode node61
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10600⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot60 run61_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution61 : Resolution where
  proof :=
    { id := 61
      source := ControlRoom.provedNode node61
      target := ControlRoom.provedNode node62
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10610⟩, ⟨10611⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot61 run62_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution62 : Resolution where
  proof :=
    { id := 62
      source := ControlRoom.provedNode node58
      target := ControlRoom.provedNode node63
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10620⟩, ⟨10621⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot58 run63_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Collect + advance"
  summary := "Clear the first lot and advance the second at the same logical tick."
  automaticOrders := [  ]

def resolution63 : Resolution where
  proof :=
    { id := 63
      source := ControlRoom.provedNode node63
      target := ControlRoom.provedNode node64
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10630⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot63 run64_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution64 : Resolution where
  proof :=
    { id := 64
      source := ControlRoom.provedNode node64
      target := ControlRoom.provedNode node65
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10640⟩, ⟨10641⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot64 run65_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution65 : Resolution where
  proof :=
    { id := 65
      source := ControlRoom.provedNode node52
      target := ControlRoom.provedNode node66
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10650⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot52 run66_0, provedTick run66_0.child run66_1, provedTick run66_1.child run66_2 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩⟩ }
  label := "Cancel backlog and cash out"
  summary := "Return the second reservation and deliver the first lot."
  automaticOrders := [ "collect", "leave" ]

def resolution66 : Resolution where
  proof :=
    { id := 66
      source := ControlRoom.provedNode node50
      target := ControlRoom.provedNode node67
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10660⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot50 run67_0, provedTick run67_0.child run67_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Finish the first lot"
  summary := "Complete one lot before deciding whether to accept another."
  automaticOrders := [ "complete" ]

def resolution67 : Resolution where
  proof :=
    { id := 67
      source := ControlRoom.provedNode node67
      target := ControlRoom.provedNode node68
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10670⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot67 run68_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver the first order"
  summary := "Collect 10 L and two service credits. Keep the operator for a possible second lot."
  automaticOrders := [  ]

def resolution68 : Resolution where
  proof :=
    { id := 68
      source := ControlRoom.provedNode node68
      target := ControlRoom.provedNode node69
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10680⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot68 run69_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Close the shift at 10 L"
  summary := "Bank two credits and return the operator. Preserve 10 L for tomorrow."
  automaticOrders := [  ]

def resolution69 : Resolution where
  proof :=
    { id := 69
      source := ControlRoom.provedNode node68
      target := ControlRoom.provedNode node70
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10690⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot68 run70_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Take the second order"
  summary := "Commit the remaining 10 L for another two service credits."
  automaticOrders := [  ]

def resolution70 : Resolution where
  proof :=
    { id := 70
      source := ControlRoom.provedNode node70
      target := ControlRoom.provedNode node71
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10700⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot70 run71_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Run the second lot"
  summary := "Use the returned labor for the final order."
  automaticOrders := [  ]

def resolution71 : Resolution where
  proof :=
    { id := 71
      source := ControlRoom.provedNode node71
      target := ControlRoom.provedNode node72
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10710⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot71 run72_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Finish the second lot"
  summary := "Advance the final unit of work. The operator remains in active custody."
  automaticOrders := [  ]

def resolution72 : Resolution where
  proof :=
    { id := 72
      source := ControlRoom.provedNode node72
      target := ControlRoom.provedNode node73
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10720⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot72 run73_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution73 : Resolution where
  proof :=
    { id := 73
      source := ControlRoom.provedNode node73
      target := ControlRoom.provedNode node74
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10730⟩, ⟨10731⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot73 run74_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution74 : Resolution where
  proof :=
    { id := 74
      source := ControlRoom.provedNode node71
      target := ControlRoom.provedNode node75
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10740⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot71 run75_0, provedTick run75_0.child run75_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Cancel active work safely"
  summary := "Return the final lot’s fuel and labor, then release the operator."
  automaticOrders := [ "leave" ]

def resolution75 : Resolution where
  proof :=
    { id := 75
      source := ControlRoom.provedNode node70
      target := ControlRoom.provedNode node76
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10750⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot70 run76_0, provedTick run76_0.child run76_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Protect the reserve"
  summary := "Cancel the second lot, return its fuel, and close with 10 L delivered."
  automaticOrders := [ "leave" ]

def resolution76 : Resolution where
  proof :=
    { id := 76
      source := ControlRoom.provedNode node67
      target := ControlRoom.provedNode node77
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10760⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot67 run77_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Queue before collection"
  summary := "Commit the remaining fuel while the first output still occupies its bay."
  automaticOrders := [  ]

def resolution77 : Resolution where
  proof :=
    { id := 77
      source := ControlRoom.provedNode node77
      target := ControlRoom.provedNode node78
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10770⟩, ⟨10771⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot77 run78_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Clear output + dispatch"
  summary := "Deliver the first lot and start the second in one simultaneous order set."
  automaticOrders := [  ]

def resolution78 : Resolution where
  proof :=
    { id := 78
      source := ControlRoom.provedNode node78
      target := ControlRoom.provedNode node79
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10780⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot78 run79_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Finish the second lot"
  summary := "Advance the final unit of work. The operator remains in active custody."
  automaticOrders := [  ]

def resolution79 : Resolution where
  proof :=
    { id := 79
      source := ControlRoom.provedNode node79
      target := ControlRoom.provedNode node80
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10790⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot79 run80_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution80 : Resolution where
  proof :=
    { id := 80
      source := ControlRoom.provedNode node80
      target := ControlRoom.provedNode node81
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10800⟩, ⟨10801⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot80 run81_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution81 : Resolution where
  proof :=
    { id := 81
      source := ControlRoom.provedNode node78
      target := ControlRoom.provedNode node82
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10810⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot78 run82_0, provedTick run82_0.child run82_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Cancel active work safely"
  summary := "Return the final lot’s fuel and labor, then release the operator."
  automaticOrders := [ "leave" ]

def resolution82 : Resolution where
  proof :=
    { id := 82
      source := ControlRoom.provedNode node77
      target := ControlRoom.provedNode node83
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10820⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot77 run83_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Dispatch into backpressure"
  summary := "Start the next lot before clearing output. You must free the bay before completion."
  automaticOrders := [  ]

def resolution83 : Resolution where
  proof :=
    { id := 83
      source := ControlRoom.provedNode node83
      target := ControlRoom.provedNode node84
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10830⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot83 run84_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Work before clearing the bay"
  summary := "Finish the work, leaving the output bay occupied. Completion will be blocked."
  automaticOrders := [  ]

def resolution84 : Resolution where
  proof :=
    { id := 84
      source := ControlRoom.provedNode node84
      target := ControlRoom.provedNode node85
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10840⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot84 run85_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Clear the output bottleneck"
  summary := "Deliver the first lot to make room for the completed second lot."
  automaticOrders := [  ]

def resolution85 : Resolution where
  proof :=
    { id := 85
      source := ControlRoom.provedNode node85
      target := ControlRoom.provedNode node86
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10850⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot85 run86_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution86 : Resolution where
  proof :=
    { id := 86
      source := ControlRoom.provedNode node86
      target := ControlRoom.provedNode node87
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10860⟩, ⟨10861⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot86 run87_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution87 : Resolution where
  proof :=
    { id := 87
      source := ControlRoom.provedNode node83
      target := ControlRoom.provedNode node88
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10870⟩, ⟨10871⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot83 run88_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Collect + advance"
  summary := "Clear the first lot and advance the second at the same logical tick."
  automaticOrders := [  ]

def resolution88 : Resolution where
  proof :=
    { id := 88
      source := ControlRoom.provedNode node88
      target := ControlRoom.provedNode node89
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10880⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot88 run89_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Complete the final order"
  summary := "Release labor and place the last output in the collection bay."
  automaticOrders := [  ]

def resolution89 : Resolution where
  proof :=
    { id := 89
      source := ControlRoom.provedNode node89
      target := ControlRoom.provedNode node90
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10890⟩, ⟨10891⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot89 run90_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Deliver + release operator"
  summary := "Finish the full 20 L shift, collect four credits, and return the operator together."
  automaticOrders := [  ]

def resolution90 : Resolution where
  proof :=
    { id := 90
      source := ControlRoom.provedNode node77
      target := ControlRoom.provedNode node91
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10900⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot77 run91_0, provedTick run91_0.child run91_1, provedTick run91_1.child run91_2 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩⟩ }
  label := "Cancel backlog and cash out"
  summary := "Return the second reservation and deliver the first lot."
  automaticOrders := [ "collect", "leave" ]

def resolution91 : Resolution where
  proof :=
    { id := 91
      source := ControlRoom.provedNode node49
      target := ControlRoom.provedNode node92
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10910⟩, ⟨10911⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot49 run92_0, provedTick run92_0.child run92_1, provedTick run92_1.child run92_2, provedTick run92_2.child run92_3, provedTick run92_3.child run92_4 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩⟩⟩⟩ }
  label := "Dispatch + depart"
  summary := "Both orders are valid now. Dispatch wins arbitration; departure loses. Inspect automatic failure and safe recovery."
  automaticOrders := [ "fail", "repair", "start", "leave" ]

def resolution92 : Resolution where
  proof :=
    { id := 92
      source := ControlRoom.provedNode node49
      target := ControlRoom.provedNode node93
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10920⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot49 run93_0, provedTick run93_0.child run93_1 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩⟩ }
  label := "Cancel the order"
  summary := "Return the reservation and release the operator."
  automaticOrders := [ "leave" ]

def resolution93 : Resolution where
  proof :=
    { id := 93
      source := ControlRoom.provedNode node48
      target := ControlRoom.provedNode node94
      sourceMember := by simp [provedNodes, nodes]
      targetMember := by simp [provedNodes, nodes]
      actionIds := [ ⟨10930⟩ ]
      actionIdsNonempty := by decide
      actionIdsUnique := by decide
      actionsAccepted := by native_decide
      steps := [ provedTick snapshot48 run94_0 ]
      stepsNonempty := by decide
      firstStepActionsExact := by native_decide
      stepsConnect := by
        simp only [CommandGraphStepsConnect]
        exact ⟨SameSnapshotData.refl _, SameSnapshotData.refl _⟩ }
  label := "Stand down"
  summary := "Return the operator and preserve the full 20 L supply."
  automaticOrders := [  ]

def resolutions : List Resolution := [ resolution0, resolution1, resolution2, resolution3, resolution4, resolution5, resolution6, resolution7, resolution8, resolution9, resolution10, resolution11, resolution12, resolution13, resolution14, resolution15, resolution16, resolution17, resolution18, resolution19, resolution20, resolution21, resolution22, resolution23, resolution24, resolution25, resolution26, resolution27, resolution28, resolution29, resolution30, resolution31, resolution32, resolution33, resolution34, resolution35, resolution36, resolution37, resolution38, resolution39, resolution40, resolution41, resolution42, resolution43, resolution44, resolution45, resolution46, resolution47, resolution48, resolution49, resolution50, resolution51, resolution52, resolution53, resolution54, resolution55, resolution56, resolution57, resolution58, resolution59, resolution60, resolution61, resolution62, resolution63, resolution64, resolution65, resolution66, resolution67, resolution68, resolution69, resolution70, resolution71, resolution72, resolution73, resolution74, resolution75, resolution76, resolution77, resolution78, resolution79, resolution80, resolution81, resolution82, resolution83, resolution84, resolution85, resolution86, resolution87, resolution88, resolution89, resolution90, resolution91, resolution92, resolution93 ]
def provedGraph : Maquina.CommandGraph Workcell.executor ControlRoom.initialState where
  actor := operatorActor
  nodes := provedNodes
  root := ControlRoom.provedNode node0
  rootMember := by simp [provedNodes, nodes]
  nodeIdsUnique := by native_decide
  candidatesOwned := by native_decide
  resolutions := resolutions.map (·.proof)
  resolutionIdsUnique := by native_decide
  resolutionChoicesUnique := by native_decide
  acceptedCandidatesCovered := by native_decide
  terminalComplete := by native_decide

/-- Every scheduled rejection is a declared arbitration conflict, never a snapshot-invalid action. -/
theorem every_rejection_is_conflict :
    (resolutions.all fun resolution => resolution.proof.steps.all fun step =>
      step.events.all fun event => match event.outcome with
        | .accepted _ => true
        | .rejected kind _ => kind == .lostConflict) = true := by native_decide

/-- The complete 20 L supply is conserved at every exported decision. -/
theorem every_snapshot_conserves_fuel :
    (nodes.all fun node =>
      let world := node.snapshot.timeline.application.accounts
      decide ((world.holdings.foldl (fun total holding =>
        if holding.resourceId = fuelId then total + holding.quantity.atoms else total) 0) = 20)) = true := by native_decide

end Maquina.Games.Foundry.Shift
